import { PAGE_PADDING } from "@/lib/theme/layout-tokens";

/* Shared horizontal padding for every landing section, so the section rhythm
 * stays consistent across the page. */
export const SECTION_PX = {
  base: PAGE_PADDING.base,
  md: PAGE_PADDING.lg,
  xl: "64px",
};
